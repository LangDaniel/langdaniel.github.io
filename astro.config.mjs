// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // required by GitHub Pages for correct canonical URLs
  site: 'https://langdaniel.github.io'
});
